#include<stdio.h>
int main(){
    int a,copy,rev=0;
    scanf("%d",&a);
    copy=a;
    while(copy>0){
        rev=rev*10;
        rev=rev+(copy%10);
        copy/=10;
    }
    if(rev==a){
      printf("Palindrome");
    }
    else printf("Not an palindrome");
}
